export type FileEntry = { path: string; directory: boolean };
export type WorkspaceView = { cwd: string; files: FileEntry[]; saved: boolean; output: string; exitCode: number; fixture: boolean; branch: string };
export type Platform = 'mac' | 'linux' | 'windows';
export type Fixture = { cwd: string; directories?: string[]; files?: Record<string, string>; repo?: string; commits?: { id?: string; message: string; files: Record<string, string> }[]; staged?: Record<string, string>; working?: Record<string, string>; setup?: string[][]; branch?: string; branches?: string[]; remotes?: Record<string, string>; tags?: string[]; reflog?: string[]; note?: string };
export type WorkspaceSnapshot = { cwd: string; files: Record<string, string>; directories: string[]; repo?: string; tracked: string[]; staged: string[]; index: Record<string, string | null>; commits: { id: string; message: string; files: Record<string, string> }[]; head: string; branch: string; branches: string[]; remotes: Record<string, string>; tags: string[]; stashCount: number };
